# Optional HTTPS front for dsh (secure context → crypto.randomUUID works on LAN IPs).
# Self-signed cert (tls internal). Browser will warn once — accept/continue.
:443 {
	tls internal
	reverse_proxy dsh:3080
}
